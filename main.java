public class Main {
  
  String[] vars = ["structural auto-evolution", "structural copying of information", "hypercycle-mediating interface", "multi-stage"];
  //non-constant (i.e. not too specific), duplication of values between variables (that is, not too vague), saving-deleting cycle (meaning carriability in memory), arrayable (in other words, must be plenty of space in program)
  //attention (thoughts), words (actions), habits (character), destiny (environment)
  public static void main(String args[]) {
    
  }
}
